services:
  phototheque:
    build: { context: .., dockerfile: deploy/Dockerfile }
    network_mode: host          # necessaire pour le mDNS/Avahi
    volumes:
      - /media/izquierdo/Famille:/data/library
      - phototheque-data:/data
    restart: unless-stopped
volumes:
  phototheque-data:
